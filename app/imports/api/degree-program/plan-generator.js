import { Courses } from '../course/CourseCollection';


export function generateCoursePlan(template, startSemester, student) {
  const plan = {};
  // year 1
  const firstSemesterCourses = template.ay1.fallSem;
}