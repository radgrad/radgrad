/* Server-side initialization code goes here. Import all server-side code. */
/* Call it index.js so that we can just import the directory name in server/main.js. */

import '/imports/api/slug/SlugCollection.js';
import '/imports/api/opportunity/OpportunityTypeCollection.js';
