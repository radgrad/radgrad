/* Client-side initialization code goes here. Import all client-side code. */
/* Call it index.js so that we can just import the directory name in client/main.js. */

import '/imports/api/slug/SlugCollection.js';
import '/imports/api/opportunity/OpportunityTypeCollection.js';
